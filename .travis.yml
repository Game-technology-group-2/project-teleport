os:
  - linux

dist: trusty
language: cpp

# compilers to add to build matrix
compiler:
  - gcc
  # - clang

# before_install:
#   - sudo apt-get update -qq
#   - sudo apt-get install -qq libsdl2-dev libsdl2-image-dev libsdl2-ttf-dev libglm-dev libglew-dev

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - cmake --build .
  - cmake --version
  